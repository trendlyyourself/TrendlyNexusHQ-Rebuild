"use client"; import {useState} from "react"; import {signIn} from "next-auth/react"; import {useRouter} from "next/navigation";
export default function Login(){const r=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [register,setRegister]=useState(false);const [msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMsg("");if(register){const x=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(!x.ok){setMsg((await x.json()).error);return}}const x=await signIn("credentials",{email,password,redirect:false});if(x?.ok)r.push("/dashboard");else setMsg("Invalid credentials.")}async function magic(){const x=await signIn("email",{email,redirect:false,callbackUrl:"/dashboard"});setMsg(x?.ok?"Check your email for the sign-in link.":"Unable to send the link.")}return <main className="flex min-h-screen items-center justify-center px-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-white/10 bg-white/[.03] p-8"><a href="/" className="text-cyan-300">← trendlynexusHQ</a><h1 className="mt-5 text-3xl font-black">{register?"Create account":"Sign in"}</h1><div className="mt-6 space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3"/><input required minLength={10} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3"/>{msg&&<p className="text-sm text-cyan-200">{msg}</p>}<button className="w-full rounded-xl bg-cyan-300 px-4 py-3 font-bold text-slate-950">{register?"Create account":"Sign in"}</button>{!register&&<button type="button" onClick={magic} className="w-full rounded-xl border border-white/10 px-4 py-3">Email me a magic link</button>}</div><button type="button" onClick={()=>setRegister(!register)} className="mt-6 text-sm text-slate-400 underline">{register?"I already have an account":"Create a new account"}</button></form></main>}
