export const dynamic = "force-dynamic";
export const runtime = "nodejs";

import {getServerSession} from "next-auth"; import {redirect} from "next/navigation"; import {authOptions} from "@/lib/auth"; import {prisma} from "@/lib/prisma";
export default async function Dashboard(){const s=await getServerSession(authOptions);if(!s?.user?.id)redirect("/login");const u=await prisma.user.findUnique({where:{id:s.user.id},include:{subscriptions:{orderBy:{createdAt:"desc"},take:1},trendMetrics:{orderBy:{score:"desc"},take:10}}});if(!u)redirect("/login");return <main className="min-h-screen"><nav className="border-b border-white/10"><div className="mx-auto max-w-6xl px-6 py-5 font-black">trendly<span className="text-cyan-300">nexus</span>HQ</div></nav><div className="mx-auto max-w-6xl px-6 py-10"><p className="text-cyan-300">CONTROL CENTER</p><h1 className="mt-2 text-4xl font-black">Welcome, {u.name||u.email}</h1><p className="mt-2 text-slate-500">Plan: <span className="text-white">{u.plan}</span></p><section className="mt-10 rounded-2xl border border-white/10 bg-white/[.03] p-6"><h2 className="text-xl font-bold">Top trend signals</h2><div className="mt-5 overflow-hidden rounded-xl border border-white/10"><table className="w-full text-left text-sm"><thead className="bg-white/5"><tr><th className="p-4">Keyword</th><th className="p-4">Score</th><th className="p-4">Volume</th><th className="p-4">Momentum</th></tr></thead><tbody>{u.trendMetrics.map(t=><tr className="border-t border-white/10" key={t.id}><td className="p-4 font-bold">{t.keyword}</td><td className="p-4">{t.score}</td><td className="p-4">{t.volume.toLocaleString()}</td><td className="p-4 text-cyan-300">+{t.momentum}%</td></tr>)}</tbody></table></div></section><form action="/api/stripe/portal" method="post" className="mt-6"><button className="rounded-xl border border-white/10 px-5 py-3">Manage billing</button></form></div></main>}
