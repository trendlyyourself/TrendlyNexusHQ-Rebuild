import {NextResponse} from "next/server"; import {getServerSession} from "next-auth"; import {authOptions} from "@/lib/auth"; import {prisma} from "@/lib/prisma"; import {getStripe} from "@/lib/stripe";
export async function POST(){const s=await getServerSession(authOptions);if(!s?.user?.id)return NextResponse.json({error:"Authentication required."},{status:401});const u=await prisma.user.findUnique({where:{id:s.user.id}});if(!u?.stripeCustomerId)return NextResponse.json({error:"No billing account."},{status:400});const p=await getStripe().billingPortal.sessions.create({customer:u.stripeCustomerId,return_url:`${process.env.NEXT_PUBLIC_APP_URL}/dashboard`});return NextResponse.json({url:p.url});}
